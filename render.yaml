services:
  - type: web
    name: ebay-business-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: sk-0ad31e2fa19943548ede1a63fb9e22d5
        sync: false  # À configurer manuellement
      - key: SESSION_SECRET
        generateValue: true
      - key: DEBUG_MODE
        value: false
